import { OverviewSection } from "@/components/dashboard/sections/OverviewSection";

/** /desk — where you act: the scan console, your open positions, and market conditions.
 * Named for what you do here, not for what it summarises; "Overview" is what licensed
 * this page to fill up with statistics instead of a primary action. */
export default function DeskPage() {
    return <OverviewSection />;
}
