import { SettingsSection } from "@/components/dashboard/sections/SettingsSection";

/** /settings — the rules your agents work inside, and where trades go. */
export default function SettingsPage() {
    return <SettingsSection />;
}
